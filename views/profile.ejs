<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white px-5">
        <a href="/logout" class="text-sm text-blue-700 px-3 flex justify-end hover:text-red-700 inline-block">Logout</a>
        <div class="flex p-4 items-center gap-3">
            <div class="w-14 h-14 rounded-full overflow-hidden">
               <a href="/profile/uplode"><img class="w-full h-full object-cover" src="/images/uplodes/<%= user.profile %>" alt=""></a> 
            </div>
            <h1 class="text-white  text-4xl font-semibold tracking-tight">Welcome to your profile, <span class="font-bold ml-1 text-sky-300"><%= user.name %></span>👋.</h1>
        </div>

        <div>
            <form action="/post" method="post">
                <textarea name="area" class="p-3 text-sm bg-transparent block rounded-lg border border-zinc-700 m-4 mt-10 w-1/3 resize-none outline-none h-32" placeholder="What's on your mind?"></textarea>
                <input type="submit" value="Upload Post" class="block bg-sky-800 px-4 py-2 rounded-lg text-sm font-semibold ml-4">
            </form>
        </div>
        <h4 class="text-zinc-600 mt-10 m-2">Your Posts.</h4>
                <div class="Post flex flex-wrap shrink-0 gap-[10px]">
                    <% user.posts.reverse().forEach(function(post){ %>
                        <div id="posts" class="w-[23%] p-2 bg-zinc-800 hover:bg-zinc-700 hover:cursor-pointer rounded-lg mb-[0.6vw]">
                            <h3 class="text-sm text-zinc-400 inline-block lowercase ">@<%= user.username %></h3>
                            <p class="text-[1.3vw] mt-1 tracking-tight"><%= post.content %></p>
                            <div class="mt-2">
                                <a href="/likes/<%= post._id %>" class="text-xs text-white mr-2 bg-blue-600 p-1 rounded-md tracking-tight">Likes</a>
                                <a href="/edit/<%= post._id %>" class="text-xs text-green-600">Edit</a>
                            </div>
                        </div> 
                 <%   }) %>       
                </div>
    </div>
</body>
</html>