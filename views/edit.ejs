<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white px-5">
        <a href="/logout" class="text-sm text-blue-700 px-3 flex justify-end hover:text-red-700 inline-block">Logout</a>


        <div>
            <form action="/update/<%= post._id %>" method="post">
                <textarea name="area" class="p-3 text-sm bg-transparent block rounded-lg border border-zinc-700 m-4 mt-10 w-1/3 resize-none outline-none h-32" placeholder="What's on your mind?"><%= post.content %></textarea>
                <input type="submit" value="Edit Post" class="block bg-sky-800 px-4 py-2 rounded-lg text-sm font-semibold ml-4">
            </form>
        </div>
        
               
    </div>
</body>
</html>